import React from 'react';
import ReactDOM from 'react-dom';

import './vendor-trash.scss';

export const VendorTrash: React.SFC = () => {
    return <div>Hello!</div>;
}

ReactDOM.render(<VendorTrash />, document.getElementById("react"));